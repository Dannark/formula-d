export class Grid {
  constructor(spacing = 20, dotSize = 2, color = "#E0E0E0") {
    this.spacing = spacing; // Espaçamento entre os pontos
    this.dotSize = dotSize; // Tamanho dos pontos
    this.color = color;     // Cor dos pontos
  }
}
