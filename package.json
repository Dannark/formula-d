{
  "name": "formula-d",
  "version": "1.0.0",
  "description": "Um jogo de tabuleiro usando ECS e Canvas 2D",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "concurrently --raw \"npm start\" \"ngrok http 1234\""
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "concurrently": "^8.2.2"
  }
}
